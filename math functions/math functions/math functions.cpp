#include <iostream>
#include <cmath>

using namespace std;

int main()
{
	double number;
	cout << "entre a numbre ?\n";
	cin >> number;
	cout << " the squar root of numbere is " << sqrt(number) << endl <<endl;
	 
	cout << " the round of the number is " << round(number) << endl << endl;

	cout << " the power of two of the number is " << pow(number, 2) << endl << endl;

	cout << " the ceiling of the number is " << ceil(number) << endl << endl;
	
	cout << " the flooring of the number is " << floor(number) << endl << endl;

	cout << " the absolute of the number is " << abs(number) << endl << endl;


	return 0;






}